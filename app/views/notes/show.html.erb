<div class="flex justify-between">
  <%= link_to "<- back", notes_path, class: 'mx-1 px-3 py-1.5 border border-gray-600 bg-gray-200 text-gray-600 rounded-md' %>
  <%= link_to "edit", edit_note_path(@note), class: 'px-3 py-1.5 text-white bg-blue-500 rounded-md' %>
</div>
<h1 class="mt-4 text-2xl font-semibold"><%= @note.title %></h1>
<p class="mt-4"><%= @note.body %></p>
