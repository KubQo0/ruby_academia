<%= form_with model: @note do |f| %>
  <% if @note.errors.any? %>
    <ul>
      <% @note.errors.each do |e| %>
        <li><%= e.full_message %></li>
      <% end %>
    </ul>
  <% end %>
  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>
  <%= f.submit submit_text %>
<% end %>